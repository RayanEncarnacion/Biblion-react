import styled from "styled-components";
import { SectionWrapper } from "../../core-ui";

export default styled(SectionWrapper)`
  font-size: 1.8em;
  margin-bottom: 1.5em;
  text-align: center;
  color: #f7f7f7;
  position: relative;
  overflow-x: hidden;

  & img {
    width: 100%;
  }
`;
